stages:
  - deploy

deploy-staging:
  stage: deploy
  image: docker:24.0.5
  script:
    - docker info
    - cd backend
    - docker-compose down --remove-orphans || true
    - docker-compose build
    - docker-compose up -d
