stages:
  - deploy

deploy-staging:
  stage: deploy
  image: docker:24.0.5
  services:
    - docker:24.0.5-dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  script:
    - cd backend
    - docker-compose down --remove-orphans || true
    - docker-compose build
    - docker-compose up -d
