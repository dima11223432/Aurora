stages:
  - deploy

deploy-staging:
  stage: deploy
  script:
    - cd backend
    - docker-compose down -v --remove-orphans || true
    - docker-compose up -d --build
    - docker image prune -f
  only:
    - PROD
