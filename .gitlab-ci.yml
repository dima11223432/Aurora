stages:
  - deploy

deploy-staging:
  stage: deploy
  script:
    - |
      cd backend
      # docker-compose down --remove-orphans || true
      docker-compose down -v || true
      docker-compose build
      docker-compose up -d
