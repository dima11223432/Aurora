stages:
  - deploy

deploy-staging:
  stage: deploy
  script:
    - cd backend
    - docker-compose down --remove-orphans
    - docker-compose build --no-cache
    - docker-compose up -d
    - docker image prune -f
  only:
    - PROD
    - main
