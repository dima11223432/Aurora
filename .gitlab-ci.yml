stages:
  - deploy

deploy-staging:
  stage: deploy
  image: docker:24.0.5
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock
  script:
    - cd backend
    - docker-compose down --remove-orphans || true
    - docker-compose build
    - docker-compose up -d
